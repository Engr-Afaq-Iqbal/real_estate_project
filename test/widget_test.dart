import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('BuildOS app smoke test', (WidgetTester tester) async {
    // Smoke test placeholder — full integration tests in integration_test/
    expect(true, isTrue);
  });
}
